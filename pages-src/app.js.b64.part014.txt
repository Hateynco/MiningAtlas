oduction.length) {
    showLoadError("Impossible de charger la base miniere.");
    refs.loading.classList.add("done");
    window.setTimeout(() => refs.loading.remove(), 400);
    return;
  }

  buildYearSelect();
  indexData();
  applyQueryState();
  bindEvents();
  refs.yearSelect.value = String(state.selectedYear);
  applyPanelState();
  renderAll();

  refs.loading.classList.add("done");
  window.setTimeout(() => refs.loading.remove(), 500);
}

boot();
